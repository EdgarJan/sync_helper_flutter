name: sync_helper_flutter
version: 1.2.3
environment:
  sdk: '>=3.8.1'
  flutter: '>=3.32.4'
dependencies:
  flutter:
    sdk: flutter
  http: 1.4.0
  sqlite_async: 0.11.7
  path_provider: 2.1.5
  uuid: 4.5.1
  package_info_plus: 8.3.0
  # Sentry for error monitoring & logging – SDK will be initialized by the
  # application that depends on this library. We only use the APIs.
  sentry_flutter: 9.0.0-RC.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0
executables:
  sync_generator: